//history.back > Loads the previous URL from the history list.
//history.forward > Loads the next URL from the history list.
//history.go > Loads a url from the history list.

document.writeln("<b>" + history + "</b>");